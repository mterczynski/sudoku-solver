import { iteratee } from 'lodash';
import { createMockEasySudoku } from '../mocks';

describe('Sudoku solver', () => {
  it('can solve easy sudoku', () => {
    const sudoku = createMockEasySudoku();

    // todo
  });
});
